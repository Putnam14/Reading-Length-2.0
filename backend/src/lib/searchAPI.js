const searchAPI = searchTerm => {
  // Query Amazon API for search term
  // Parse results
  // const book = ctx.db.mutation.createBook(...parsedData)
  // return book.isbn
  const isbn = "0553593714";
  console.log("Searching");
  return isbn;
};

module.exports = searchAPI;
